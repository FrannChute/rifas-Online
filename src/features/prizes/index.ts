import type { FeatureDefinition } from "@/features/feature-types";

export const prizesFeature = {
  key: "prizes",
  title: "Premios",
  owner: "admin",
  stage: "active",
} satisfies FeatureDefinition;
